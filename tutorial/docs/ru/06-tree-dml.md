# Tree DML: insertJSON и upsertJSON

Цель главы: разобрать запись дерева данных за один запрос и понять алгоритм `upsertJSON`.

## Что важно сразу

- `insertJSON` и `upsertJSON` работают с root + nested children.
- Оба пути используют один SQL-пайплайн записи дерева.
- Ключевое отличие между ними — ограничения на вход.

## `insertJSON`: строгая вставка

Правила:
- на каждом узле дерева должны быть переданы все обязательные поля;
- для каждой строки генерируется только `INSERT` (без `UPDATE` и без
  `ON CONFLICT`); дубликат по PK или unique — ошибка PostgreSQL.

Используйте, когда создаёте новые данные. Для идемпотентной заливки или
обновления по ключу — `upsertJSON`.

## `upsertJSON`: вставка или обновление

Правило для каждой строки:
- либо есть полный mandatory-набор,
- либо есть полный первичный ключ или полный уникальный ключ из схемы
  (сначала ограничения, у которых все колонки `NOT NULL`; если таких UK нет —
  уникальные ограничения с nullable-колонками).

Цель выбора ключа в SQL для __`upsertJSON`__: первый из списка «PK, затем
NOT NULL UK, затем nullable UK (только если NOT NULL UK нет)», для которого в типе
узла есть все колонки ключа. При `NULL` в значении nullable-колонки ключа
семантика определяется PostgreSQL (`UNIQUE` vs `NULLS NOT DISTINCT`).
Ветка `UPDATE` сравнивает ключевые колонки через `IS NOT DISTINCT FROM`.
## Алгоритм upsertJSON (практическая модель)

1. Проверить форму входного дерева на уровне типов.
2. Для каждого узла определить стратегию идентификации (mandatory или полный PK / подходящий UK).
3. Построить SQL для root и всех дочерних веток.
4. Выполнить операцию как один round-trip к БД.
5. Вернуть результат в типизированной форме.

## Returning и ограничение поддерева

Практическое правило:
- возвращаемое дерево должно быть поддеревом входного дерева.

Это снижает риск “невалидного ожидания” результата и проверяется compile-time.

## Почему это важно (perf + transactional)

- Один запрос вместо нескольких ручных шагов.
- Меньше накладных расходов на сетевые round-trip.
- Проще сохранить целостность операции в рамках одной БД-команды.

## Мини-checklist после главы

- [ ] Понимаете разницу `insertJSON` vs `upsertJSON`.
- [ ] Понимаете правило «mandatory или полный PK / подходящий unique».
- [ ] Понимаете ограничение returning как subtree input.

## Что дальше

- Что уже умеем: писать и обновлять дерево данных типобезопасно.
- Что пока ограничивает: не разобраны типичные сложные ошибки и редкие кейсы.
- Дальше: в `07-compiler-errors-as-guide` разберем диагностику compile-time ошибок.
